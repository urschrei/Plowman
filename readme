# Description #

A python script designed to tweet the contents of a text file, line by line. Line position is stored in a SQlite database. The script is configured to take account of header lines, and thus maintain line numbering.